# 安全说明（Security Policy）

## 报告漏洞

**请不要在公开 Issue 中报告安全漏洞。**

发现安全问题请通过以下渠道私密报告：

- GitHub 私密漏洞报告：仓库页面 → **Security → Report a vulnerability**
- 或通过邮件联系维护者（见仓库主页）

## 安全设计基线

本工具的核心安全机制：

1. **API 密钥认证**：使用 unRAID 官方 API 密钥（非密码），建议使用最小所需角色
2. **操作确认门**：所有写操作（容器管理 / 部署 / 卸载）必须显式确认，未确认自动拒绝
3. **输出脱敏**：密钥 / 密码 / token 在输出中自动打码
4. **审计日志**：所有操作留痕于 `~/.unraid/audit.log`（600 权限）
5. **命令注入防护**：所有进入 SSH 命令的容器名 / 应用名经过白名单校验
6. **版本门槛**：unRAID <7.2 自动拒绝（无内置 API）

## 权限建议

- **只读查询**：使用 VIEWER 角色密钥即可
- **容器管理 / 部署**：需要 ADMIN 角色 —— 请评估信任边界后使用
- 危险操作（电源管理 / 阵列变更）**不在本工具范围**，人工处理

## 使用风险声明

> ⚠️ 本工具具有修改 NAS 状态的能力。请勿将未经验证的 Agent 会话授予生产数据权限；
> 部署第三方容器前请核实镜像来源（默认强制 CA 官方应用 + 来源分级提示）。

## 已知限制

- unRAID <7.2 无内置 API，工具拒绝运行
- 密钥以文件形式存储于 `~/.unraid/keys/`（600 权限），请确保运行用户可信
