---
name: Bug 报告
about: 报告问题以帮助我们改进
title: "[BUG] "
labels: bug
assignees: ''
---

## 环境信息（必填）
- unRAID 版本：`（如 7.3.2，`unraid_api.py --compat` 可查）`
- API 组件版本：`（--compat 输出中的 api 值）`
- 系统：`（如 Ubuntu 24.04 / macOS / Windows WSL）`
- Python 版本：`python3 --version`
- 模块：`（如 info_array.py / op_deploy.py / install.sh）`

## 问题描述
清晰描述发生了什么。

## 复现步骤
1. 执行命令：`python3 xxx.py ...`
2. 预期行为：
3. 实际行为：

## 输出/日志（脱敏后）
```
粘贴输出，注意抹掉 API 密钥等敏感信息
```

## 自检情况
- [ ] `python3 unraid_api.py --compat` 是否全绿？（是/否/未运行）
- [ ] 是否已确认版本在兼容表内（7.2+）？

## 其他
